defaultNamespace: kube-system

labels:
  app: kubevip

helm:
  repo:  https://kube-vip.github.io/helm-charts
  chart: kube-vip
  version: 0.4.2
  releaseName: kubevip
  values:
    env:
      vip_interface: "eth0"
      vip_address: "10.11.0.80"
      cp_enable: "true"